apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitlab_exam.fullname" . }}-test-connection"
  labels:
    {{- include "gitlab_exam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitlab_exam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
